{
  "name": "was-today-better-than-yesterday-alexa",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/klausbayrhammer/was-today-better-than-yesterday-alexa.git",
  "author": "Klaus Bayrhammer <bayrhammer.klaus@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "expect": "^23.6.0",
    "mocha": "^5.2.0",
    "proxyquire": "^2.1.0"
  },
  "dependencies": {
    "request-promise": "^4.2.2"
  },
  "scripts": {
    "deploy": "eval $(assume-role kb-private) && serverless deploy",
    "deploy-code": "eval $(assume-role kb-private) && serverless deploy --function handler"
  }
}
